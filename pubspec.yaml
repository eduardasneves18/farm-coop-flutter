name: coop_farm
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  generics_components_flutter:
    git:
      url: https://github.com/eduardasneves18/generics_components_flutter.git
  firebase_core: ^3.12.1
  firebase_database:
  firebase_auth: ^5.5.1
  english_words: ^4.0.0
  provider: ^6.0.0
  mobx: ^2.0.0
  webview_flutter: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true